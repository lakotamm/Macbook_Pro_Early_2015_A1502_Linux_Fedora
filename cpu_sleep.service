[Unit]
Before=sleep.target
Description=Turn CPUs offline before sleep

[Service]
Type=oneshot
ExecStart=/bin/bash -c 'for i in {1..3}; do echo 0 > /sys/devices/system/cpu/cpu$i/online; done'

[Install]
WantedBy=sleep.target
